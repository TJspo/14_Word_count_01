import streamlit as st  ## 機動できなければ、pip install streamlit を実行
import pandas as pd
import numpy as np

st.title('Streamlit 超入門')  ## ターミナルで接続してないとstremaliはリロードできない
                             ## ローカルで実施している状態。webで公開していくGithubを通してシェアリングシステムを使う
st.write('DataFrame')